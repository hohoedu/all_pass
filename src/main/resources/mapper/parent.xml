<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hohoedu.all_pass.family.repository.ParentRepository">

    <insert id="insert" parameterType="com.hohoedu.all_pass.family.Parent" useGeneratedKeys="true"
            keyProperty="id" keyColumn="id">
     INSERT INTO erp_parent (parent_name, parent_tel_first, parent_tel_middle, parent_tel_last, relation_key, parent_privacy_agree, signature, student_id, created_at)
        VALUES (#{parentName}, #{parentTelFirst}, #{parentTelMiddle}, #{parentTelLast}, #{relationKey}, #{parentPrivacyAgree},#{signature}, #{studentId}, SYSDATETIME());
    </insert>
    

</mapper>