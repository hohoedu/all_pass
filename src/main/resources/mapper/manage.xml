<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hohoedu.all_pass.manage.repository.ManageRepository">

    <select id="findBasicOrderList" resultType="com.hohoedu.all_pass.manage._dto.ManageRespDTO$BasicOrderListDTO">
        SELECT t.class_key,
               c.class_name,
               t.unit_key,
               u.unit_name,
               COUNT(ta.student_id) AS base_count
        FROM erp_time_table t
                 JOIN erp_time_table_assign ta ON t.time_table_key = ta.time_table_key
                 JOIN erp_class_code c ON t.class_key = c.class_key
                 JOIN erp_unit_code u ON t.unit_key = u.unit_key
        WHERE t.center_code = #{centerCode}
          AND t.user_code = #{userCode}
          AND t.yy = #{yy}
          AND t.mm = #{mm}
        GROUP BY t.class_key, c.class_name, t.unit_key, u.unit_name
    </select>

    <select id="findSavedOrderList" resultType="com.hohoedu.all_pass.manage._dto.ManageRespDTO$SavedOrderListDTO">
        SELECT o.base_count  AS baseCount,
               o.add_count   AS addCount,
               o.base_count AS totalCount,
               o.yy,
               o.mm,
               o.class_key   AS classKey,
               c.class_name  AS className,
               o.unit_key    AS unitKey,
               u.unit_name   AS unitName
        FROM erp_order o
                 JOIN erp_class_code c ON o.class_key = c.class_key
                 JOIN erp_unit_code u ON o.unit_key = u.unit_key
        WHERE center_code = #{centerCode}
          AND user_code = #{userCode}
          AND yy = #{yy}
          AND mm = #{mm}
    </select>

    <insert id="insertReorder">
        INSERT INTO erp_reorder (user_code, center_code, yy, mm, reorder_type, class_key, unit_key, count, reason, created_at, confirmed)
        VALUES (#{userCode}, #{centerCode}, #{yy}, #{mm}, #{reorderType}, #{classKey}, #{unitKey}, #{count}, #{reason}, SYSDATETIME(), 'unchecked')
    </insert>

    <select id="findReorderList" resultType="com.hohoedu.all_pass.manage._dto.ManageRespDTO$ReorderListDTO">
        SELECT r.id         AS id,
               reorder_type AS reorderType,
               r.class_key  AS classKey,
               c.class_name AS className,
               r.unit_key   AS unitKey,
               u.unit_name  AS unitName,
               reason       AS reason,
               count,
               confirmed,
               created_at   AS createdAt
        FROM erp_reorder r
                 JOIN erp_class_code c ON r.class_key = c.class_key
                 JOIN erp_unit_code u ON r.unit_key = u.unit_key
        WHERE center_code = #{centerCode}
          AND user_code = #{userCode}
          AND confirmed NOT IN ('user_cancel')
          AND yy = #{yy}
          AND mm = #{mm}
    </select>

    <insert id="insertOrderHistory">
        INSERT INTO erp_order_history(center_code, user_code, yy, mm, class_key, unit_key, count, created_at) VALUES
        <foreach collection="insertOrders" item="order" separator=",">
            (#{centerCode}, #{userCode}, #{yy}, #{mm}, #{order.classKey}, #{order.unitKey}, #{order.baseCount}, SYSDATETIME())
        </foreach>
    </insert>

    <select id="findOrder" resultType="com.hohoedu.all_pass.manage._dto.ManageRespDTO$BaseOrderListDTO">
        SELECT *
        FROM erp_order
        WHERE center_code = #{centerCode}
          AND user_code = #{userCode}
          AND yy = #{yy}
          ANd mm = #{mm}
    </select>

    <delete id="deleteOrderByCondition">
        DELETE
        FROM erp_order
        WHERE center_code = #{centerCode}
          AND user_code = #{userCode}
          AND yy = #{yy}
          AND mm = #{mm}
    </delete>

    <insert id="insertOrder">
        INSERT INTO erp_order(center_code, user_code, yy, mm, class_key, unit_key, base_count, total_count,created_at) VALUES
        <foreach collection="insertOrders" item="order" separator=",">
            (#{centerCode}, #{userCode}, #{yy}, #{mm}, #{order.classKey}, #{order.unitKey}, #{order.baseCount}, #{order.baseCount}, SYSDATETIME())
        </foreach>
    </insert>

    <select id="findClassFeeMapByCenterCode" resultType="com.hohoedu.all_pass.payment._dto.web.PaymentRespDTO$ClassFeeMapDTO">
        SELECT cf.class_key  AS classKey,
               cf.unit_key   AS unitKey,
               cf.fee        AS fee,
               cf.category   AS category,
               cc.class_name AS className,
               cc.class_type AS classType,
               uc.unit_name  AS unitName
        FROM erp_class_fee_map_copy cf
                 JOIN erp_class_code cc ON cf.class_key = cc.class_key
                 LEFT JOIN erp_unit_code uc ON cf.unit_key = uc.unit_key
        WHERE cf.center_code = #{centerCode}
    </select>

    <insert id="insertClassFeeMap" parameterType="java.util.List">
        INSERT INTO erp_class_fee_map (class_key, unit_key, center_code, fee)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.classKey}, #{item.unitKey}, #{item.centerCode}, #{item.fee})
        </foreach>
    </insert>

    <update id="updateClassFeeMap" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            UPDATE erp_class_fee_map
            SET fee = #{item.fee}
            WHERE class_key = #{item.classKey}
            AND center_code = #{item.centerCode}
            AND (
            (unit_key IS NULL AND #{item.unitKey} IS NULL)
            OR unit_key = #{item.unitKey}
            )
        </foreach>
    </update>

    <select id="findAllClassNames" resultType="map">
        SELECT class_key  as classKey,
               class_name as className
        FROM erp_class_code
    </select>

    <select id="findAllUnitNames" resultType="map">
        SELECT unit_key  as unitKey,
               unit_name as unitName
        FROM erp_unit_code
    </select>

    <update id="cancelReorder">
        UPDATE erp_reorder
        SET confirmed = 'user_cancel'
        where id = #{id}
    </update>

</mapper>