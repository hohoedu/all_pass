<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hohoedu.all_pass.admin.AdminRepository">
    <update id="insertPersonYear">
        MERGE INTO erp_person_year AS target
            USING (SELECT #{centerCode} AS center_code,
                          #{yy}         AS yy,
                          #{mm}         AS mm,
                          #{classKey}   AS class_key) AS src
            ON (
                target.center_code = src.center_code
                    AND target.yy = src.yy
                    AND target.mm = src.mm
                    AND target.class_key = src.class_key
                )
            WHEN MATCHED THEN
                UPDATE SET
                    unit_key = #{unitKey},
                    sub_unit_key = #{subUnitKey}

            WHEN NOT MATCHED THEN
                INSERT (center_code, yy, mm, class_key, unit_key, sub_unit_key)
                    VALUES (#{centerCode}, #{yy}, #{mm}, #{classKey}, #{unitKey}, #{subUnitKey});
    </update>

    <select id="selectPersonYear" resultType="com.hohoedu.all_pass.admin._dto.AdminRespDTO$PersonYearDTO">
        SELECT center_code, yy, mm, class_key, unit_key, sub_unit_key
        FROM erp_person_year
        WHERE center_code = #{centerCode}
          AND yy = #{yy}
        ORDER BY mm, class_key;
    </select>

    <!-- 이북 코드 생성: 지점/연월 시간표에 편성된 교재 x 호수 목록 -->
    <select id="selectKeycodeList" resultType="com.hohoedu.all_pass.admin._dto.AdminRespDTO$KeycodeDTO">
        WITH CLASS_KEY AS (SELECT DISTINCT CASE
                                               WHEN LEFT(t.class_key, 1) = 'N'
                                                   THEN SUBSTRING(t.class_key, 2, LEN(t.class_key))
                                               ELSE t.class_key
                                               END AS class_key,
                                           t.unit_key
                           FROM erp_time_table t
                           WHERE t.center_code = #{centerCode}
                             AND t.yy = #{yy}
                             AND t.mm = #{mm}
                             AND t.class_key IN ('Y', 'S', 'P', 'K', 'M', 'J',
                                                 'SU', 'DU',
                                                 'ES', 'EP', 'EG', 'ED',
                                                 'NES', 'NEP', 'NEG', 'NED'))
        SELECT ck.class_key   AS classKey,
               cc.class_name  AS className,
               cc.class_type  AS classType,
               ck.unit_key    AS unitKey,
               uc.unit_name   AS unitName,
               CASE WHEN uc.unit_type = 'lv' THEN 1 ELSE 0 END AS levelUnit
        FROM CLASS_KEY ck
                 JOIN erp_class_code cc ON ck.class_key = cc.class_key
                 JOIN erp_unit_code uc ON ck.unit_key = uc.unit_key
        ORDER BY cc.id, uc.id;
    </select>

    <!-- 외부 DB 교재 코드 → 우리 class_key -->
    <select id="selectSecondaryClassMap" resultType="map">
        SELECT ext_code  AS extCode,
               class_key AS classKey,
               ocode     AS ocode
        FROM erp_secondary_class_map
        WHERE center_code = #{centerCode}
    </select>

    <!-- 유곡점(세컨더리) 결과를 우리 코드 체계로 옮길 때 사용 -->
    <select id="selectClassCodes" resultType="com.hohoedu.all_pass.class_instance.model.ClassCode">
        SELECT id, class_key AS classKey, class_name AS className, class_type AS classType
        FROM erp_class_code
        ORDER BY id
    </select>

    <select id="selectUnitCodes" resultType="com.hohoedu.all_pass.class_instance.model.UnitCode">
        SELECT id, unit_key AS unitKey, unit_name AS unitName, unit_type AS unitType
        FROM erp_unit_code
        WHERE unit_type &lt;&gt; 'lv'
        ORDER BY id
    </select>

    <select id="findSubject" resultType="com.hohoedu.all_pass.admin.model.SubjectCode">
        SELECT *
        FROM erp_subject_code
    </select>

    <select id="findBookSuggest" resultType="com.hohoedu.all_pass.admin._dto.AdminRespDTO$BookSuggestViewDTO">
        SELECT *
        from erp_book_suggest
    </select>

    <select id="findBookSuggestByMonth" resultType="com.hohoedu.all_pass.admin._dto.AdminRespDTO$BookSuggestViewDTO">
        SELECT class_key      AS classKey,
               yy,
               mm,
               week,
               subject_key    AS subjectKey,
               book_name      AS bookName,
               publisher,
               book_image_url AS bookImageUrl
        FROM erp_book_suggest
        WHERE class_key = #{classKey}
          AND yy = #{yy}
          AND mm = #{mm}
    </select>

    <update id="upsertBookSuggest">
        MERGE INTO erp_book_suggest AS target
            USING (SELECT #{classKey} AS class_key,
                          #{yy}       AS yy,
                          #{mm}       AS mm,
                          #{week}     AS week) AS src
            ON target.class_key = src.class_key
                AND target.yy = src.yy
                AND target.mm = src.mm
                AND target.week = src.week
            WHEN MATCHED THEN
                UPDATE SET
                    subject_key = #{subjectKey},
                    book_name = #{bookName},
                    publisher = #{publisher},
                    book_image_url = #{bookImageUrl},
                    updated_at = SYSDATETIME()
            WHEN NOT MATCHED THEN
                INSERT (subject_key, book_name, publisher, book_image_url, class_key, yy, mm, week, created_at)
                    VALUES (#{subjectKey}, #{bookName}, #{publisher}, #{bookImageUrl}, #{classKey}, #{yy}, #{mm},
                            #{week}, SYSDATETIME());
    </update>

    <update id="updateOrderDeadline" parameterType="com.hohoedu.all_pass.admin._dto.AdminReqDTO$OrderDeadlineDTO">
        UPDATE erp_order_deadline
        SET dead_line_at = #{deadline}
        WHERE center_code = #{centerCode}
    </update>
</mapper>